{
  "name": "figma-svg-to-react",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "turbo test",
    "build": "turbo build",
    "dev": "turbo develop",
    "lint": "biome lint --write ./apps ./shared",
    "format": "biome check --write ./apps ./shared && pnpm biome format --write ./apps ./shared"
  },
  "devDependencies": {
    "turbo": "^2.1.2"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@biomejs/biome": "^1.9.2"
  }
}
